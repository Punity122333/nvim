# tools/snipe.lua

Source: `lua/plugins/tools/snipe.lua`

## Purpose
Custom picker/search integration used by keymaps.

## Plugins
- `Punity122333/snipe.nvim`

## Configuration
- Loads from `master` branch with `keys = false`.
- Setup invoked explicitly via `require("snipe").setup()`.
