# ui/devicons.lua

Source: `lua/plugins/ui/devicons.lua`

## Purpose
Icon configuration for filetypes and extensions.

## Plugins
- `nvim-mini/mini.icons`
- `nvim-tree/nvim-web-devicons` (disabled)

## Configuration
- Adds `regex` extension glyph via `mini.icons`.
- Disables `nvim-web-devicons` in favor of `mini.icons`.
